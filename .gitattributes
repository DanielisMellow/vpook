# Normalize line endings to LF in the repo to avoid CRLF/LF churn
# (this repo is edited from both Windows and WSL).
* text=auto eol=lf

# Treat binary assets as binary
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.gif binary
